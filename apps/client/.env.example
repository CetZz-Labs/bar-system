VITE_API_URL=http://localhost:3000/api

# LB-80 — Web Push. MISMA clave pública VAPID que el backend
# (`apps/server/.env` → VAPID_PUBLIC_KEY). Par de DESARROLLO; regenerar por
# entorno con `npx web-push generate-vapid-keys`.
VITE_VAPID_PUBLIC_KEY=
