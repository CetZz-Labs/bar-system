# Dependencies
node_modules
.pnp
.pnp.js

# Local env files
.env
.env.local
.env.*.local

# Testing
coverage

# Build outputs
dist
build
out
.next
.turbo
.vercel

# Debug / OS
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.DS_Store
*.pem
.atl

# Nested lockfiles (monorepo uses root lockfile only)
apps/**/pnpm-lock.yaml
apps/**/package-lock.json
apps/**/yarn.lock

# Graphify AST graphs (regenerable; not SSOT — see ../graphify/README.md)
graphify-out/

# AI agent harness — per-developer working memory (progress/history.md and
# progress/current.template.md stay tracked; see progress/current.template.md)
progress/current.md
progress/explorers/*
progress/implementers/*
progress/reviewers/*
!progress/explorers/.gitkeep
!progress/implementers/.gitkeep
!progress/reviewers/.gitkeep
